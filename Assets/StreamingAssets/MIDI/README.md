Coloque arquivos MIDI (.mid) nesta pasta para incluí-los no build.

Para testes no Editor você também pode usar a pasta externa padrão configurada em `ConfigService`:

  C:\Users\Murillo\Music\MIDI

Se preferir, altere o caminho via `ConfigService.SetMusicFolderPath(path)` no Inspector ou em tempo de execução.
